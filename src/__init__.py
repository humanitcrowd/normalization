"""CharLUFS package.

`__version__` is the single in-process source of truth for the app version.
Keep it in sync with `pyproject.toml` and the two `setup.py` plist entries
when bumping.
"""

__version__ = "1.4.1"
